name: checksRequired
on:
    pull_request:
      branches: [ main ]

jobs:
  checksRequired:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: |
          echo "Running tests..."
    