name: checks
on:
    pull_request:
      branches: [ "main" ]
    merge_group:
      branches: ["main"]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: |
          echo "Running tests..."
    